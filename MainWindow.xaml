<Window x:Class="TodoList.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TodoList"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition Width="280"/>
            </Grid.ColumnDefinitions>
            <TextBox Name="tbItemToAdd" Margin="15,7,15,7"/>
            <Button Grid.Column="1" Name="btnAddItem" Content="Add" Width="80" Click="btnAddItem_Click" Margin="5,0,5,0"/>
            <Button Grid.Column="1" Name="btnRemoveItem" Content="Remove" Click="btnRemoveItem_Click" Margin="90,0,5,0" Width="80"/>
            <Button Grid.Column="1" Name="btnClearAllItems" Content="Clear" Click="btnClearAllItems_Click" Width="80" Margin="175,0,0,0"/>
        </Grid>
        <ListView Grid.Row="1" Name="lvEntries" Margin="15" ItemsSource="{Binding Entries}"/>
    </Grid>
</Window>
