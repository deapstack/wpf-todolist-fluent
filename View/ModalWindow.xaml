<Window x:Class="TodoList.View.ModalWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TodoList.View"
        mc:Ignorable="d"
        Title="ModalWindow" Height="200" Width="400"
        WindowStartupLocation="CenterOwner">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <TextBox Name="txtInput" Width="300" Height="40" Margin="5" TextChanged="txtInput_TextChanged"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center">
            <Button Name="btnOK" Content="OK" Width="80" Height="35" Margin="5"
                    Click="btnOK_Click"
                    IsEnabled="False"/>
            <Button Name="btnCancel" Content="Cancel" Width="80" Height="35" Margin="5"
                    Click="btnCancel_Click"/>
        </StackPanel>
        
    </Grid>
</Window>
